package com.poleschuk.cafe.model.entity;

/**
 * UserState enum represents state of a user.
 */
public enum UserState {
    
    /** New user. */
    NEW,

    /** Active user. */
    ACTIVE,

    /** Blocked user. */
    BLOCKED;

}
